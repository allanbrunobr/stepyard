Fix ONLY the lint errors shown below.

Rules:
- Do NOT change any logic or behavior
- Do NOT add new features
- Do NOT reformat code that isn't part of the error
- Fix the minimum required to make lint pass
{% if stack is defined %}
- You are working on a {{ stack.name }} project — apply idiomatic fixes for this ecosystem
{% endif %}

## Lint Output
{{ steps.run_lint.stdout }}
